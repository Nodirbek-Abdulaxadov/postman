@inherits LayoutComponentBase
@inject CustomerSession Session
@inject NavigationManager Navigation

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4 justify-content-between">
            <span>
                @if (Session.CurrentUser is null)
                {
                    <span class="text-muted small">Not logged in</span>
                }
                else
                {
                    <span class="small">@Session.CurrentUser.FullName</span>
                }
            </span>

            @if (Session.IsAuthenticated)
            {
                <button class="btn btn-sm btn-outline-secondary" @onclick="Logout">Logout</button>
            }
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">&#x2715;</a>
</div>

@code {
    private void Logout()
    {
        Session.Clear();
        Navigation.NavigateTo("/login", forceLoad: false);
    }
}
